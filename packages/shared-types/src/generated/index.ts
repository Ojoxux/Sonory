/**
 * OpenAPI から自動生成された型定義。
 * 手動編集禁止 — `npm run generate:types` で再生成する。
 */
export type {
   components as HonoApiComponents,
   paths as HonoApiPaths,
} from "./hono-api.js"

export type {
   components as AudioAnalyzerComponents,
   paths as AudioAnalyzerPaths,
} from "./audio-analyzer.js"
