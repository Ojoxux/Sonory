/**
 * 確認完了画面の型定義
 *
 * @description
 * 確認完了画面のステータスとイベントを管理
 *
 * @param className 追加のCSSクラス
 * @param isClosing クローズアニメーション中かどうか
 */

export type ConfirmationCompleteProps = {
  className?: string
  isClosing?: boolean
}
