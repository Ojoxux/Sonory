{
	"name": "@sonory/python-audio-analyzer",
	"version": "0.1.0",
	"description": "YAMNet-based audio analysis service for Sonory",
	"private": true,
	"scripts": {
		"python:install": "python3 -m pip install -e .[dev]",
		"python:dev": "python3 -m uvicorn src.main:app --reload --host 0.0.0.0 --port 8000",
		"python:lint": "ruff check . && mypy .",
		"python:format": "black . && isort .",
		"python:test": "pytest",
		"python:build": "docker build -t sonory-audio-analyzer .",
		"generate-types": "echo 'Types will be generated by python-types package'"
	}
}
